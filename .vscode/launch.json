{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and run Raylib Chess",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/chess.exe",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/mingw64/bin/gdb.exe",
            "preLaunchTask": "Build Raylib Chess"
        }
    ]
}
